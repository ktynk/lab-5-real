#include<stdio.h>
#include<stdlib.h>
int main(){
int i,j,x,y;
int arr[2][2];
for (i=0;i<2;i++){

 for (j=0;j<2;j++)

 {

 printf("Vvedite element [%d,%d]\n", i+1, j+1);

 scanf("%f",&b[i][j]);

 }
for (x=0;x<2;i++)
{
int s=0;
 for (y=0;y<2;j++)
 s+=arr[x][y];
 printf("Summa elementov %d-stroki matrici = %.2f\n", x+1, s);
 }
 
//int main()
{

	int arr[2][2];
	for (int i = 0; i < 2; i++) 
	{
		for (int z = 0; z < 2; z++)
		{
			cout << "vvedite element pod indeksami: " << i << " " << z << endl;
			cin >> arr[i][z];
		}
	}
	for (int i = 0; i < 2; i++) 
	{
		int summa = 0;
		for (int z = 0; z < 2; z++) 
		{
			if (arr[i][z] < 0) {
				z = 2;
				summa = 0;
			}
			else
			{
				summa += arr[i][z];
			}
		}
		if (summa != 0) {
			cout <<"summa ne otricatelnih: "<< summa << endl;
		}
	}
	
	
	return 0;
   
}
